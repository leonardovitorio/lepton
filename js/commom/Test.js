import { assert } from './assert.js'

export class Test{
    assert(result, msgSucess, msgError){
        assert(result, msgSucess, msgError)
    }
    run(){
        throw 'Implements run method to test!!!'
    }
}